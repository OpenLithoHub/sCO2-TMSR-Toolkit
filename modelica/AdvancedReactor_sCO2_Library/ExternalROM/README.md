# ExternalROM — drop-zone for FMUs

> **Reference:** docs/02_phase2_cfd_rom.md § 2.6.4 + docs/03 § 3.3.
>
> Place `PCHE_ROM_FMU.fmu` here once `rom/exported/wrap_as_fmu.py` has been
> built (`pythonfmu build -f wrap_as_fmu.py`). The Modelica `PCHE.mo`
> component picks the FMU up from this directory when `useROM = true`.

🚧 Empty until Phase 2 month 8 milestone (ROM training pipeline validated +
FMU export verified in OpenModelica).
